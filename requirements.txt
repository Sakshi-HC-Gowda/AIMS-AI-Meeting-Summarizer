# Core Dependencies
streamlit>=1.50.0
nltk>=3.9.2
pdfplumber>=0.11.7
pypdf2>=3.0.1
python-docx>=1.2.0
reportlab>=4.4.4
scikit-learn>=1.7.2
spacy>=3.8.7

# HTTP requests for Ollama API
requests>=2.31.0

# ML/AI Dependencies (Required for summarization)
transformers>=4.30.0
torch>=2.0.0
sentencepiece>=0.1.99
protobuf>=3.20.0

# Audio Processing (Optional - for audio transcription)
# vosk: Fast, lightweight offline speech recognition
# vosk>=0.3.45
# faster-whisper: Optimized Whisper - 4-5x faster, runs offline (alternative)
faster-whisper>=1.0.0
pyaudio>=0.2.13  # For live audio recording

# Note: openai-whisper removed - use faster-whisper instead
# faster-whisper is compatible with openai-whisper's interface

# Speaker Diarization (Optional - for better speaker identification)
pyannote.audio>=3.1.0
pyannote.core>=5.0.0

# Additional dependencies for transformers
accelerate>=0.20.0
huggingface-hub>=0.16.0

# Sentence Transformers (Optional - for embeddings)
sentence-transformers>=2.2.0

# Additional utilities
numpy>=1.24.0
pandas>=2.0.0
librosa>=0.10.2
soundfile>=0.12.1

